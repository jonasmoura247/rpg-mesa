// Placeholder — a lógica real de rolagem de tabelas será implementada na Task 12.
const Rolador = {
  rolar(nomeSecao, idNota, tituloTabela, containerResultado) {
    containerResultado.innerHTML = '<p class="resultado-rolagem">🎲 Rolador ainda não implementado (em breve).</p>';
  },
};
